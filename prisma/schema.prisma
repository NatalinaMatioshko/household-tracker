// Minimal MVP schema for Household Tracker.
// Auth.js wiring comes in Phase 3 — User exists so Product can own userId.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Category {
  HYGIENE
  CARE
}

model User {
  id           String    @id @default(cuid())
  email        String    @unique
  name         String?
  /// Filled in Phase 3 (Auth.js email/password). Nullable until then.
  passwordHash String?
  products     Product[]
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}

model Product {
  id        String     @id @default(cuid())
  userId    String
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  name      String
  category  Category
  purchases Purchase[]
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt

  @@index([userId])
}

model Purchase {
  id            String    @id @default(cuid())
  productId     String
  product       Product   @relation(fields: [productId], references: [id], onDelete: Cascade)
  price         Float
  quantity      Int       @default(1)
  datePurchased DateTime  @db.Date
  dateEnded     DateTime? @db.Date
  notes         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  @@index([productId])
}
